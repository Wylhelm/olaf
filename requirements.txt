crewai[tools]   # Core CrewAI framework with tools support
python-dotenv   # Environment variable management
requests        # HTTP client for API interactions
openai          # OpenAI API integration for GPT models
plotly          # Interactive visualization library
pandas          # Data manipulation and analysis
jinja2          # Required for HTML template rendering
anthropic       # Required for Claude models
chromadb        # Required for database operations
pyyaml          # Required for YAML configuration files
plotly          # Required for reports generation
